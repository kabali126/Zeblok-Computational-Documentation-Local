---
title: "Python SDK"
description: "Control the Ai-MicroCloud platform from Python — every component, one client."
---

The Zeblok Python SDK (version 2.0.0) lets you drive the Ai-MicroCloud platform from code.
Anything you can do in the web interface — create plans, start workstations, deploy
microservices, serve AI models, run agents — you can do from a script.

## Install

```bash
pip install "zeblok-sdk[all]"
```

Python 3.9 or newer. The `[all]` extra adds the optional AWS and Azure object-store
backends and Socket.IO push updates. Plain `pip install zeblok-sdk` covers everything else.

## Connect

Everything hangs off a single client. Create it once, then reach each component from it.

```python
import os

from zeblok import ZeblokClient

client = ZeblokClient(
    os.environ["ZBL_APP_URL"],        # e.g. "https://app.your-env.zeblok.com"
    os.environ["ZBL_ACCESS_KEY"],
    os.environ["ZBL_ACCESS_SECRET"],
)

print(client.health())    # {'status': 'ok', ...}  -> platform reachable
print(client.config())    # {'tier': 'pro', ...}   -> your key works
```

<Tip>
  Paste whatever URL you use in the browser. The web app and the REST API are served from
  different hosts, and which prefix is correct differs per environment — the client checks
  and switches for you.
</Tip>

## How every page here is organised

Each component page follows the same shape, so once you learn one you know them all:

| Part | What it gives you |
|---|---|
| **Capabilities** | A table of every operation, the SDK call, and the REST endpoint behind it |
| **Examples** | Ready-to-copy code for the common paths |
| **Notes** | The behaviour worth knowing before you hit it |

<Warning>
  **Two kinds of call.** Reads (`get_all`, `get_by_id`, `list`, `validate_id`) only look —
  run them freely. Writes (`create`, `update`, `delete`, `spawn`, `stop`) change real things
  and consume real resources.
</Warning>

## Components

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/sdk/authentication">
    The three credentials, what each is for, and how to verify you are connected.
  </Card>
  <Card title="Quickstart" icon="rocket" href="/sdk/quickstart">
    A read-only first script that proves your setup and prints the ids you need.
  </Card>
  <Card title="Plans" icon="server" href="/sdk/plans">
    Define how much CPU, GPU, memory and storage a workload gets.
  </Card>
  <Card title="Namespaces" icon="folder" href="/sdk/namespaces">
    Isolate workloads and control who can reach them.
  </Card>
  <Card title="Data centers" icon="building" href="/sdk/datacenters">
    Inspect machines and check free capacity before you deploy.
  </Card>
  <Card title="Workstations" icon="laptop-code" href="/sdk/workstations">
    Start a JupyterLab development machine in the cloud.
  </Card>
  <Card title="Microservices" icon="cubes" href="/sdk/microservices">
    Deploy an application and keep it running.
  </Card>
  <Card title="Orchestration add-ons" icon="diagram-project" href="/sdk/orchestration-addons">
    Run cluster tools such as Ray for distributed compute.
  </Card>
  <Card title="Inference" icon="brain" href="/sdk/inference">
    Serve an AI model as a live endpoint and chat with it.
  </Card>
  <Card title="Agents" icon="robot" href="/sdk/agents">
    Run AI assistants with tools, and delegate between them.
  </Card>
  <Card title="AI Model Hub" icon="download" href="/sdk/model-hub">
    Import models from HuggingFace and manage them.
  </Card>
  <Card title="Datasets" icon="database" href="/sdk/datasets">
    Store files on the platform and get them back.
  </Card>
  <Card title="Ai-API & Pipelines" icon="arrows-turn-right" href="/sdk/ai-api-pipelines">
    Turn a folder of your own code into something running on the platform.
  </Card>
  <Card title="Containerization" icon="box" href="/sdk/containerization">
    Watch the image builds the platform runs for you.
  </Card>
  <Card title="Workload lifecycle" icon="rotate" href="/sdk/lifecycle">
    One handle to stop, start, resize, share and delete anything you started.
  </Card>
  <Card title="IAM" icon="user-shield" href="/sdk/iam">
    Users, roles, groups, policies and organisations.
  </Card>
  <Card title="Bulk operations" icon="layer-group" href="/sdk/bulk-operations">
    Create many things at once without half-finished runs.
  </Card>
  <Card title="Errors" icon="triangle-exclamation" href="/sdk/errors">
    What each message means and what to do about it.
  </Card>
</CardGroup>
